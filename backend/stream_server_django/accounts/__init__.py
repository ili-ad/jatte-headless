# LEGACY APP:
# This `accounts` app is an older implementation kept for reference.
# New code should use `stream_server_django.accounts_supabase` instead.

default_app_config = 'stream_server_django.accounts.apps.AccountsConfig'
