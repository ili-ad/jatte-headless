{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "dev": {
      "dependsOn": ["^dev"],
      "outputs": [],
      "script": "turbo-dev"
    },    
    "build": { "outputs": [".next/**", "dist/**"] },
    "test":  { "outputs": [] }
  }
}
